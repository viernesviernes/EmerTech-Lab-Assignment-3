{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run deploy --prefix auth-app\" \"npm run deploy --prefix community-app\" \"npm run dev --prefix shell-app\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
